include _common

-
  var tickets = [
    {
      img: 'normal',
      link: 'fake-url-normal-ticket'
    },
    {
      img: 'VIP',
      link: 'fake-url-VIP-ticket'
    }
  ]

mixin ticket-unit(ticket)
  .ticket-unit
    .image(style=`background-image: url(${imagePath}tickets/ticket_${ticket.img}.png)`)
    a.text.pf-Medium(href=`${ticket.link}`, target="_blank").
      立即购票


#tickets
  +title-bar('参会购票')
  .tickets-wrapper
    each ticket in tickets
      +ticket-unit(ticket)
  .control-panel
    .bar-dots
      - for (var i = 1; i<=2; i++)
        if (i===2)
          .dot-unit.selected
        else
          .dot-unit
    .bar-info
      .arrow
        .arrow-unit.left
      .info-wrapper
        .info-text
          .text.
            *购票说明：本项目为全国联网销售，与现场座位一一对应，采用【短信电子票】模式，请用户在购买流程中身份证一栏填写准确的手机号。请于演出当天，持官方通知短信，至演出现场换票入场。
      .arrow
        .arrow-unit.right
